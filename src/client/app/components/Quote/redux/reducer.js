const INITIAL_STATE = {
  data: {
    quote: 'I am a riddle, or a quote',
  },
  config: {
    position: {
      bottom: 60,
      left: 60,
    },
    components: {},
    dimensions: {},
  },
};

export default (state = INITIAL_STATE) => state;
